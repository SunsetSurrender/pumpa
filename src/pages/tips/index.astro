---
import { getCollection } from 'astro:content';
import SiteLayout from '../../layouts/SiteLayout.astro';

const posts = (await getCollection('tips', ({ data }) => !data.draft))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const fmt = (d: Date) =>
  d.toLocaleDateString('en-GB', { day: '2-digit', month: 'short', year: 'numeric' });
---
<SiteLayout
  title="Tips — Pumpa"
  description="Practical articles on fuel economy, fuel prices and commute costs — the math behind cheaper driving."
  surface="reading"
>
  <div class="page-head">
    <p class="mono-label">Tips</p>
    <h1>Drive the same. Pay less.</h1>
    <p class="lede">
      Short, practical reads on fuel economy, pricing quirks and commute math —
      each one something you can act on before your next fill-up.
    </p>
  </div>

  <div class="tips-list">
    {posts.map((post) => (
      <a class="site-card tip-card" href={`/tips/${post.id}/`}>
        <div class="tip-meta">
          <time datetime={post.data.pubDate.toISOString()}>{fmt(post.data.pubDate)}</time>
          {post.data.tags.map((tag) => <span class="tip-tag">{tag}</span>)}
        </div>
        <h2>{post.data.title}</h2>
        <p>{post.data.description}</p>
        <span class="tip-more">Read →</span>
      </a>
    ))}
  </div>
</SiteLayout>

<style>
  .tips-list{
    display: grid; grid-template-columns: repeat(2, 1fr);
    gap: 18px; margin-top: 28px;
  }
  .tip-card{
    display: block; text-decoration: none; color: inherit;
    background: var(--reading-surface);
    border-color: var(--reading-border);
    box-shadow: none;
    transition: border-color .15s;
  }
  .tip-card:hover{ border-color: var(--reading-link); }
  .tip-meta{
    display: flex; align-items: center; gap: 10px; flex-wrap: wrap;
    font-size: 0.8125rem;
    color: var(--reading-text-mid); margin-bottom: 0.75rem;
  }
  .tip-tag{
    border: 1px solid var(--reading-border); border-radius: 999px;
    padding: 2px 10px; color: var(--reading-text-mid);
  }
  .tip-card h2{
    margin: 0 0 0.5rem; font-size: 1.25rem; line-height: 1.3;
    color: var(--reading-text);
  }
  .tip-card p{ margin: 0 0 1rem; font-size: 0.9375rem; line-height: 1.6; color: var(--reading-text-mid); }
  .tip-more{
    font-size: 0.9375rem; font-weight: 600;
    color: var(--reading-link);
  }
  @media (max-width: 760px){
    .tips-list{ grid-template-columns: 1fr; }
  }
</style>
